# 実装指示書: 人狼英語リスニング特訓アプリ `listen.html`

## 目的

オーナー（英語学習者・日本人）が、ワンナイト人狼で使う英語フレーズ102個を
「文字を見ずに聴き取れる」＋「暗記して口から出せる」状態にするための自習アプリ。
スマホのブラウザで使う。生徒用クイズ（index.html）とは別の独立ページ。

## 作るもの（成果物は1ファイルのみ）

- `listen.html` — 完全自己完結の1ページアプリ（CSS/JSすべてインライン、外部CDN禁止）
- 既存ファイルは一切変更しない（_redirects は別途こちらで対応済み）

## データ

`listening-2026-07.json` を `fetch('listening-2026-07.json')` で読む（同階層）。構造:

```json
{
  "version": 1,
  "title": "人狼英語 リスニング特訓",
  "cats": [{"key": "claim", "jp": "役職CO"}, ...8種],
  "phrases": [
    {"id":"c01","en":"I claim Seer.","jp":"占い師をCOします。","cat":"claim","catJp":"役職CO","tier":1,"audio":"/audio/ls/c01.mp3","note":"任意の補足"}
  ],
  "pairs": [
    {"id":"pr01","focus":"単数か複数か","jp":"解説文",
     "a":{"en":"I checked the center card.","audio":"/audio/ls/pr01a.mp3"},
     "b":{"en":"I checked the center cards.","audio":"/audio/ls/pr01b.mp3"}}
  ]
}
```

phrases=102個（tier1=44, tier2=44, tier3=14）、pairs=10組。noteは無い項目もある。

## 音声の扱い（重要・このサイト特有の罠）

- このサイトの `_headers` は全パス `Cache-Control: no-store`。**Audioのsrcに直接URLを渡すと毎回再取得になる**
- 対策: 起動時に対象デッキのMP3を `fetch` → `blob` → `URL.createObjectURL` でメモリに先読みする
  （既存 `index.html` の `preloadAllAudio` と同じ方式。読み込み中は「音声を準備中… n/122」の進捗表示）
- 再生: `new Audio(blobUrl)` を使い回し。**速度は `audio.playbackRate`**（0.6 / 0.8 / 1.0）。
  `audio.preservesPitch = true` と `audio.webkitPreservesPitch = true` を明示的にセット（音程を保ったまま遅くする）
- 全122ファイル・合計約1.5MB程度なので全部先読みでよい

## 進捗保存（localStorage）

キー `jinrohListen.v1` にJSONで保存。毎操作後に即保存:

```js
{
  "flags": { "c01": {"miss": true, "memo": false}, ... },  // 記録がある id のみ
  "pairStats": { "pr01": {"ok": 3, "ng": 1}, ... },
  "speed": 0.8,
  "deck": { "tier": "1", "filter": "all", "cat": "all" }
}
```

- **miss（⚠️ミス）**: 聞き取れなかった・間違えたフレーズの印。手動トグル＋自動ON（後述）
- **memo（✅暗記OK）**: 「見ずに口からサッと言える」の自己申告。**手動トグルのみ**（自動では絶対に変えない）

## 画面構成（SPA・画面切替はJSで）

### 1. ホーム画面

- タイトル「人狼英語 リスニング特訓」
- 進捗サマリー: 「✅暗記OK 12 / 102」「⚠️ミス中 5」（現在のデッキでなく全体の数字）
- デッキ設定（選択はlocalStorageに保存し次回復元）:
  - レベル: 「Tier1だけ(44)」「Tier1+2(88)」「全部(102)」
  - 絞り込み: 「すべて」「⚠️ミスだけ」「未暗記だけ」
  - カテゴリ: 「すべて」＋8カテゴリ（役職CO/夜の情報/疑い/質問/弁明/投票/会話・あいづち/進行・結果）
- モードボタン4つ（大きく・縦積み）:
  1. 🎧 聞き取りカード
  2. 👂 聞き分けドリル（単数複数など）
  3. ❓ どの文？4択
  4. 📋 フレーズ一覧
- デッキが0件になる組み合わせの場合はモードボタンを無効化し「該当フレーズがありません」と表示

### 2. 聞き取りカードモード

フラッシュカード式。デッキをシャッフルして1枚ずつ。

- 上部: 「3 / 44」カウンター、ホームに戻るボタン
- 速度切替（セグメントボタン、常時表示）: 「🐢超スロー 0.6」「🚶ゆっくり 0.8」「🏃ふつう 1.0」→ 選択は保存
- カード表示時に**自動で1回再生**。「▶ もう一回」ボタンで何度でも再生
- 「答えを見る」ボタン → 英文＋日本語訳＋note（あれば）を表示
- 答え表示後に自己判定ボタン2つ:
  - 「○ 聞き取れた」→ 次のカードへ（missフラグは変更しない）
  - 「× ダメだった」→ **missフラグを自動ON**して次のカードへ
- カード下部に手動トグル2つ（答え表示前でも操作可）: 「⚠️ミス」「✅暗記OK」
  現在の状態が見た目でわかるように（ONは色付き）
- デッキを一周したら「一周完了！○=32 ×=12」のまとめ→「もう一周」「ホームへ」

### 3. 聞き分けドリルモード

10組のペアからランダム出題（偏らないように循環シャッフル）。

- aかbをランダムに再生（自動1回＋「▶もう一回」・速度切替あり）
- 画面に2つの大きなボタンで両方の英文を表示 → 聞こえた方をタップ
- 判定後: 正解／不正解の表示＋ペアの解説文（jp）を表示＋両方の音声を聞き比べボタン（「▶ Aを聞く」「▶ Bを聞く」）
- pairStatsに ok/ng を記録。画面上部に「このペアの正答率: 3/4」を表示
- 「次へ」で次の問題

### 4. どの文？4択モード

- デッキからランダムに1文再生（自動1回＋もう一回・速度切替）
- 選択肢4つ: 正解＋同カテゴリ優先のダミー3つ（足りなければ他カテゴリから）。英文のみ表示
- タップで判定。**不正解なら正解フレーズのmissを自動ON**
- 判定後に正解の英文＋日本語訳を表示、「次へ」
- セッション正答数「8/10」を上部に表示

### 5. フレーズ一覧

手動チェックのメイン画面。デッキ設定（レベル・絞り込み・カテゴリ）が効く。

- カテゴリ見出しごとにグループ表示
- 各行: 英文（メイン）／日本語訳（小さく）／tierバッジ（T1/T2/T3）／「▶」再生ボタン／
  「⚠️」「✅」トグル（タップで即切替・即保存）
- 行の英文をタップすると日本語訳の表示/非表示を切替（デフォルト表示でよい）

## デザイン

- 既存サイトと同系のダークテーマ: 背景 `#0d0d14`、本文 `#e8e2d0`、控えめな金 `#c9a44a` をアクセント、
  カードは `#16161f` 程度のパネル。フォントは `'Noto Sans JP', sans-serif`（システムフォールバックでよい）
- スマホ最優先: `max-width: 560px` 中央寄せ、ボタンは高さ48px以上、文字は大きめ
- ⚠️ミスON=赤系（`#c0504a`程度）、✅暗記OK=緑系（`#5a9a6e`程度）で状態が一目でわかる
- 派手なアニメは不要。実用重視

## その他の要件

- `<html lang="ja">`、viewport設定、タイトル「人狼英語 リスニング特訓」
- OGP: og:title 同じ、og:description「ワンナイト人狼の英語フレーズ102個を聴き取り練習」
- 音声の自動再生はユーザー操作（モード開始ボタンのタップ）の後なのでブロックされないが、
  万一 `play()` がrejectされたら「▶」タップを促す表示にフォールバック
- localStorageが壊れていた場合（JSON.parse失敗）は初期化して動き続ける
- 依存ライブラリなし。素のJS（ES2020程度まで可）

## 検証（実装後に必ず）

1. `node --check` 相当でJS構文エラーがないこと（インラインJSを一時ファイルに切り出してチェックしてよい）
2. `python3 -m http.server` で配信し `curl` で200が返ること
3. listening-2026-07.json の全id（c01〜g17、pr01a〜pr10b）とaudioパスの整合を目視確認
